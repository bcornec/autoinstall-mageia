------------------------------------------------------------------------------
| Images      | Action                                                       |
| ----------- | ------------------------------------------------------------ |
| local       | Boot on local disk (default mode)                            |
|----------------------------------------------------------------------------|
{% for node in nodes -%}
{% if node.name != 'system' and node.name != 'dploy' -%}
{% for machine in node.nodenames -%}
| {{ machine.name }}      | Installation of Mageia {{ node.name }} node {{ machine.name }} ({{ node.mgaver }} {{ node.mgaarch }}) |
{% endfor -%}
{% endif -%}
{% endfor -%}
------------------------------------------------------------------------------

