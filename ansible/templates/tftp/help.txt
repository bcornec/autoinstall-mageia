------------------------------------------------------------------------------
| Images      | Action                                                       |
| ----------- | ------------------------------------------------------------ |
| local       | Boot on local disk (default mode)                            |
|----------------------------------------------------------------------------|
{% for item in nodes -%}
{% if item.name != 'system' and item.name != 'dploy' -%}
{% for machine in item.nodenames -%}
| {{ machine.name }}      | Installation of Mageia {{ item.name }} node {{ machine.name }} ({{ item.mgaver }} {{ item.mgaarch }}) |
{% endfor -%}
{% endif -%}
{% endfor -%}
------------------------------------------------------------------------------

