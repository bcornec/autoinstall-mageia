#
# Shorewall -- /etc/shorewall/snat
#
# For information about entries in this file, type "man shorewall-snat"
#
# See https://shorewall.org/manpages/shorewall-snat.html for more information
#
###################################################################################################################################################
# Shouldn't be harcoded and also adapted for difrent node types
#ACTION                 SOURCE                  DEST            PROTO   DPORT   SPORT   IPSEC   MARK    USER    SWITCH  ORIGDEST        PROBABILITY
{% set net_mask = MGAMACHINEINET ~ '/' ~ MGAMACHINEIMASK %}
# {{ net_mask }} => {{ net_mask | ansible.utils.ipaddr('prefix') }}
MASQUERADE      {{ MGAMACHINEINET }}/{{ net_mask | ansible.utils.ipaddr('prefix') }}               {{ MGAMACHINEPUBITF }}
